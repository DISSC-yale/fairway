# Build + caches
build/
__pycache__/
*.pyc
*.egg-info/
.coverage
coverage.json
coverage.xml
htmlcov/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local-only
.DS_Store
.fairway_cache/
.venv/

# v0.2 era directories (left untracked)
.reports
codemaps
teams
/site/
/data/
/logs/
/manifest/
mock_apptainer
tests/output
config/

# User notes (not tracked by policy)
/notes/
notes

# Environment + secrets
.env
.env.*

# Forge debate review snapshots leaked to repo root
.review_*.py

# Conversation exports (large, scratch)
2026-*-git-pub*.txt

# Per-user agent state — DO commit .claude/{commands,hooks}, NOT these
.claude/settings.local.json
.claude/worktrees/
